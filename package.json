{
  "name": "ticket-transfer-mailer",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prepare:web": "node scripts/prepare-web.mjs",
    "cap:sync": "npm run prepare:web && npx cap sync android",
    "cap:sync:ios": "npm run prepare:web && npx cap sync ios",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "dotenv": "^17.4.2",
    "express": "^4.21.2",
    "nodemailer": "^6.9.16",
    "pdfkit": "^0.18.0"
  }
}
